streamlit==1.18.0
requests==2.28.2
hashlib==0.0.1  # hashlib is part of the standard library, so this may not be needed
subprocess==0.0.1  # subprocess is also a part of the standard library, so this may not be necessary
email-validator==1.3.0
